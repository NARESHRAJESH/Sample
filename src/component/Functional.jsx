function Functional(props){
    return(
        <>
        <h1>Hello {props.name}</h1>
        </>
    )
}
export default Functional

//does not store the state(data)
//simpler
//storage - not taken

//props - one to one component, read only
